mutation CreatePaymentIntent($input: CreatePaymentInput!){
    initPayment(input:$input){
        customerId
        ephemeralKey
        paymentIntent{
            id
            amount
            currency
            status
            secret
        }
    }
}